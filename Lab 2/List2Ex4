ping google.com | grep -o "time.*" | tr [:lower:] [:upper:]

#Mam mały problem z tym zadaniem. Jeżeli dobrze rozumiem piping powyższy potok powinien wyświetlać "TIME=x MS", jednakże w konsoli nic się nie wyświetla. Pojedynczy piping (ping google.com | grep -o "time.*") dla zarówno jednej jak i drugiej komendy wyswietla output na ekranie konsoli.
